{# <nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container-fluid">
    <a class="navbar-brand" href="#"> 
      <img src="{{ asset('build/images/vinyl.svg') }}" class="vinyl" alt="icone styles" >
    </a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
      <span class="line1"></span>
      <span class="line2"></span>
      <span class="line3"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
      <div class="navbar-nav">
        <a class="nav-link active" aria-current="page" href="#">Home</a>
        {% for style in styleRepository.findAll() %}
        <a href="" class="nav-link">{{style.name}}</a>
        {% endfor %}
      </div>
      <div class="navbar-nav">
        <a href="" class="nav-link">Inscription</a>
        <a href="" class="btn btn-success">Login</a>
        <a href="" class="btn btn-danger">Logout</a>
      </div>
    </div>
  </div>
</nav> #}
<nav>
  <div class="logo">
      <a class="navbar-brand" href="#"> 
        <img src="{{ asset('build/images/vinyl.svg') }}" class="vinyl" alt="icone styles" >
      </a>
  </div>
  <ul class="nav-links">
    {% for style in styleRepository.findAll() %}
      <li><a href="" class="nav-link">{{style.name}}</a></li>
    {% endfor %}
  </ul>
  <div class="burger">
    <div class="line1"></div>
    <div class="line2"></div>
    <div class="line3"></div>
  </div>
</nav>